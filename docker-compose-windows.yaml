services:
  mt5:
    build: .
    image: gmag11/metatrader5_vnc
    environment:
      - PUID=0
      - PGID=0
    volumes:
      - mt5-config:/config
    env_file:
      - .env
volumes:
  mt5-config:
